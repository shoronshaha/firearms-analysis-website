import React from 'react';

const Recharts = ({ rechart }) => {
    const { id, month, investment, sell, revenue } = rechart
    return (
        <div>

        </div>
    );
};

export default Recharts;